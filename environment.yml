name: particle-vtools
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip:
      - ipykernel
      - ipython
      - numpy
      - pandas
      - natsort
      - pyvista
      - matplotlib
      - imagecodecs
      - scikit-image
      - trame
      - ipywidgets
